import React from "react";
import ReactDOM from "react-dom/client";
import RateTracker from "./RateTracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RateTracker />
  </React.StrictMode>
);
